Setup instructions are from - https://www.waveshare.com/wiki/Servo_Driver_HAT

Install libraries

```
sudo apt-get updata
sudo apt-get install python-pip 
sudo pip install RPi.GPIO
sudo apt-get install python-smbus
```

